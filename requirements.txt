# The scoring logic (configuration, profile, scoring engine, recommendation
# engine, synthetic data) runs on the standard library alone. Everything below
# is for the export service and the dashboard.

# Testing
pytest>=8.0

# Export service -- PDF and Excel downloads
reportlab>=4.1
openpyxl>=3.1

# Dashboard
streamlit>=1.38
